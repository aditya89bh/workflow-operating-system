"""Standalone demonstration of the decision intelligence layer.

Run it directly:

    python examples/decision_demo.py

It mirrors the ``workflow-os decision-demo`` command.
"""

from workflow_os.decision.demo import run_demo

if __name__ == "__main__":
    run_demo()
