"""Standalone demonstration of the SOP memory layer.

Run it directly:

    python examples/sop_demo.py

It mirrors the ``workflow-os sop-demo`` command.
"""

from workflow_os.sop.demo import run_demo

if __name__ == "__main__":
    run_demo()
